{
  "name": "savr-monorepo",
  "version": "1.0.0",
  "description": "SAVR - Smart AI Virtual Recipes",
  "scripts": {
    "deploy": "npm run build --prefix web && firebase deploy --only hosting,storage,firestore && firebase deploy --only functions",
    "deploy:hosting": "npm run build --prefix web && firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:delete-functions": "firebase functions:delete analyzeImage createRecipe createMealPlan createGroceryList chat createStripeCheckout stripeWebhook createStripePortal uploadLabelingImage getImageAnnotations saveAnnotation triggerSegmentation exportDataset --region us-central1 --force"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/InTellMe/SAVR.git"
  },
  "keywords": [
    "firebase",
    "recipes",
    "ai",
    "meal-planning"
  ],
  "author": "InTellMe",
  "license": "MIT"
}
